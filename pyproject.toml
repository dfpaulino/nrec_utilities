[project]
name = "nrec-utils"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "dpaulino", email = "dfpaulino@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
nrec-utils = "nrec_utils.main:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
